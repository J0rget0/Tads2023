def soma(a: float, b:float = 5) -> float:
    """ Sum two values.

    Take two variables and sum.

    Args:
        a (float): First number.
        b (float, optional): Second number. Defaults to 5.

    Returns:
        float: The sum of the two values.
    """
    num = a + b
    
    return num